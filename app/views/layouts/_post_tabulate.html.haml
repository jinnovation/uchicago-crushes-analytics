.scrollable
  %table.table.table-striped

    - @posts.sort_by {|post| post.fb_created_time}.reverse.each do |post|
      %tr
        %td
          .media
            = link_to holder_tag("100x100", "User pic", class: "media-object thumbnail img-responsive"), "#", class: "pull-right"
            .media-body
              .media-heading

                -# TODO: on click, launch modal containing the facebook post
                %h4= post.fb_created_time.strftime("%B %d, %Y %l:%M %p")
              = link_to post.content, post.post_url, target: "_blank"
