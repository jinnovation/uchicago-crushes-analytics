.thumbnail
  - @img = if not user.nil? then image_tag(user.pic_url_medium) else holder_tag("100x100", "Unknown") end
  = link_to @img, user, { "data-toggle"=>"modal", "data-target"=>"#homepage-info-modal" }
  .caption.center
    %h4= if user.nil? then "" else user.full_name end
    %h5= if user.nil? then "" else Crush.find_by_user_id_and_post_id(user.id, post.id).quotient.to_s end
