%p#notice= notice

.row
  .col-lg-4
    %h1= "#{@user.name}"
    %h2= "#{pluralize(@user.posts.length, "post")}"
    .center
      = link_to image_tag(@user.pic_url), @user.profile_url, target: "_blank"
    
  .col-lg-8
    %table.table.table-striped
      - @user.posts.each do |post|
        %tr
          %td= post.content

= link_to 'Back', users_path
